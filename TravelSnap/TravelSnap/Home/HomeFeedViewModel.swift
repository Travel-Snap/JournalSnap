//  HomeFeedViewModel.swift

import Foundation

// HomeFeedViewModel.swift

import Combine
import SwiftUI

class HomeFeedViewModel: ObservableObject {
    
    //I didn't delete this view model because the screens are not ready yet so maybe we need it for other stuff.
    //I deteletd the dummy data though. I created some entries on the firebase so we can have something to work on it until Abby finishes the create view.
    

}

